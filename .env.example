# SHA-256 hex digest only. Never store the plain administrative token.
VITE_ADMIN_TOKEN_SHA256=
# Public Worker URL only; never put Worker secrets or access codes in this file.
VITE_PUBLICATION_API_URL=https://YOUR-WORKER.workers.dev/
